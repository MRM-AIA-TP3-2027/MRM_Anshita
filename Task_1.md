Task 1: Make a folder on your local system called GitHub_task and make a program in any language of your choice which prints the statement “Hello World!” inside said folder. Push the folder into the GitHub repository just created using the Linux command line.

In task 1 I created a folder called GitHub_task on my local system and created a python code file with the code "Hello World!" in it all using terminal.

After this I initialised a Git repository and connected the local repository to github.

then I added and committed the file and then pushed it to github

GIT COMMANDS USED:

- git init
- git remote add origin https://github.com/MRM-AIA-TP3-2027/MRM_Anshita.git
- git add .
- git commit -m "Task1: Hello World"
- git push -u origin main
